<!-- class Solution {
public:
    bool isAnagram(string s, string t) {

    //Method - 1
        
     if( s.length() != t.length()){
        return false;
     }
     sort(s.begin(), s.end());
     sort(t.begin(), t.end());
     return s==t;
    }

    //Method - 2
    
    if( s.length() != t.length()){
        return false;
    }

    map<char, int> count;
    for (int i = 0; i < s.size(); i++){
        count[s[i]]++;
    }

    for (int i = 0; i < t.size(); i++) {
            if (--count[t[i]] < 0) {
            return false;
        }
    }

    return true;
    }
}; -->